package firstStepsMyProject

fun main() {

   // For lists
    var list1 = mutableListOf(1, 2, 3, 4) // Mutable = editable (grow or reduce?)
    list1.add(5)

    list1 = mutableListOf<Int>(6, 7)

    /**
     * Read product and store the value inside a val
     * Read price and store the value inside a val
     * Check whether any of the above values is null
     * If any of the values is null... Ask the user again?...
     * If both values are okay, store them in the lists respectively
     *
     *
     * Banana: $1.50
     * Pear: $2.80
     * Apple: $1000
     * Number of produces: 3
     *
     *
     * Options:
     * 1 bla bla
     * 2 bla bla
     * 3 bla bla
     *
     * Please enter you option: ____
     *
     *
     */

    try {
        // Your code
    } catch (exception: NumberFormatException) {
        // Your code when there is an error
    }


}